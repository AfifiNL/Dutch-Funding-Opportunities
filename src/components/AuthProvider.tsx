'use client';

import { AuthProvider as ContextAuthProvider } from '@/contexts/AuthContext';

// Re-export the AuthProvider component from our context
export const AuthProvider = ContextAuthProvider; 